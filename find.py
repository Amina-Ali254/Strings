# find(sub[start[end]])

# The functions is used to search for the first occurrence of the substring in the given string. It returns the index 
# at which the substring starts. It returns -1 if the substring does occur in the string.

A = "Save Earth"
print(A.find('ar'))

A = "Save Earth"
print(A.find('p'))