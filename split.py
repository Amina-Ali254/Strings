# split([sep])

# The functions splits the string into substrings using the seperator. The second argument is optional and its default 
# value is zero. If an integer value N is given for the second argument, the string is split into N+1 strings.

str = 'The$earth$is$what$we$all$have$in$common'
print(str.split('$'))
